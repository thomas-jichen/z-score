{
  "name": "z-score",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3737",
    "build": "next build",
    "build:check": "NEXT_DIST_DIR=.next-check next build",
    "start": "next start -p 3737",
    "check": "tsx scripts/check-parsing.ts"
  },
  "dependencies": {
    "next": "^16.3.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "//overrides": "postcss and sharp arrive as transitive deps of next, pinned below their patched versions. Forcing them here clears the audit without a Next major bump; both are build-time only.",
  "overrides": {
    "postcss": "^8.5.26",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "tsx": "^4.23.12",
    "typescript": "^5.9.2"
  }
}
